# Worker requires the full API dependency set because it imports from app/
# (app.celery_app, app.tasks.processing, app.core.config, etc.)
# Web server packages (uvicorn, gunicorn) are excluded — not needed for a worker process.

# Database
sqlalchemy==2.0.25
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Redis
redis==5.0.1
hiredis==2.3.2

# Celery
celery==5.3.6
kombu==5.3.5

# Pydantic & Validation
pydantic==2.6.1
pydantic-settings==2.1.0
email-validator==2.1.0.post1

# Security (imported transitively via app.core)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
python-dotenv==1.0.1
httpx==0.26.0
psutil==5.9.8

# Document Processing & Storage
boto3==1.34.34
python-magic==0.4.27
PyMuPDF==1.23.26
langdetect==1.0.9

# TTS
google-cloud-texttospeech==2.16.3

# Audio Processing
pydub==0.25.1
mutagen==1.47.0
ffmpeg-python==0.2.0

# Monitoring & Observability
prometheus-client==0.20.0
sentry-sdk==1.40.6

# Billing
stripe==8.2.0
